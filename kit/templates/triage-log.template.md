# Triage Log — <mission title>

<!-- Orchestrator-only. One entry per discoveredIssue from worker handoffs that did NOT become a fix feature. Blocking issues become kind:"fix" features in features.json instead of (or in addition to) an entry here. -->

<!-- Entry format — copy per issue:

## <ISO timestamp> — <feature-id that reported it>

- **Severity**: blocking | non_blocking | suggestion
- **Issue**: <description as reported in the handoff>
- **Disposition**: fix-feature <new-feature-id> | deferred | rejected | already-fixed
- **Rationale**: <why this disposition>
-->
